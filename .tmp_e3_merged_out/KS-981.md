**Ticket Title:** `Dynamo MCP QA - Validate list_table, describe_table, read_data`

**Jira:** [KS-981](https://gendvn.atlassian.net/browse/KS-981) | **Epic:** Dynamo MCP — Functional E2E Validation

**User Story:**

> As an **Internal QA Tester**, I want **table listing, schema description, and first 10 rows** so that **tabular access matches described schema**, while **explicitly recording results for HIGH security-risk tools** per guide **section 1.4**.

**Overview:**

section 5.5 validates discovery (`list_table`, `describe_table`) and tabular read (`read_data`) for internal consistency. **These three tools are HIGH risk** (schema/tabular exposure) and **may be removed or restricted in production** — track pass/fail separately on Conceptia; use **S (skipped)** if not in build scope.

**Detailed Requirements:**

* **Security (section 1.4):** Maintain a **High-risk tool checklist** row for `list_table`, `describe_table`, `read_data` in the test report.
* **Prompt (example):** _List the available data tables, describe the structure of the funds table, then read the first 10 rows._
* **Consistency:** Column names, types, and descriptions from `describe_table` must match the shape of data returned by `read_data`.
* **Row limit:** Confirm behavior at 10 rows (or tool default); document if server caps differ.
* **Authorization:** Data must remain within the **authorized MCP/user scope** (black box — no UI cross-check).

**Acceptance Criteria (BDD):**

_Scenario 1 — Happy path_

* **Given** `list_table` returns a set including a funds-related table
* **When** the tester describes that table and reads the first 10 rows
* **Then** row data conforms to described columns and types, and values are **plausible** (no requirement to compare to an external system)

_Scenario 2 — Error path_

* **Given** an invalid table name passed to `describe_table` or `read_data`
* **When** the tester invokes the tool
* **Then** schema validation or a clear API error is returned; no partial dump of unrelated tables

_Scenario 3 — Edge case_

* **Given** a table with more than 10 rows or very wide rows
* **When** the tester reads the first 10 rows
* **Then** only 10 (or documented default) rows return, performance is acceptable

**Definition of Done:** _(verbatim block above)_

---

---

## Updated requirements — guide v1.4 (8-tool MCP inventory)

**Effective:** May 2026 (customer confirmation)  
**Sources:** `Dynamo Server/Test Guide/dynamo-mcp-testing-guide_v1.4.md`; `Dynamo Server/Test Guide/dynamo_mcp_testing_stories_v1.2.md`  
**Scope note:** This section **supplements** the description above. It aligns the ticket with **US-E3-05** in stories v1.2 even if the Jira summary still mentions legacy discovery tools.

### User Story (v1.4)

> As an **Internal QA Tester**, I want **`read_data` tabular reads executed only when registered** so that **guide section 5.5 and section 1.4 HIGH-risk tracking** are satisfied on the **8-tool** surface—without `list_table` / `describe_table` (not in v1.4 inventory).

### Overview (v1.4)

**Guide section 5.5 — Tabular read test (`read_data`)** is **in scope** only when **`read_data`** appears in the MCP client registry. Until then: **S (skipped)** with reason *not yet deployed / not registered*—per **[KS-991](https://gendvn.atlassian.net/browse/KS-991)** and guide section 1.4.

**Out of scope (v1.4):** `list_table`, `describe_table`, `search_aloha_funds`, ratings tools. The **HIGH** risk row in the security checklist applies to **`read_data` only** within the 8-tool set.

| Tool | v1.4 status |
| --- | --- |
| `read_data` | **Planned** — full 5.5 execution when registered |
| `list_table` / `describe_table` | **Not in inventory** — do **not** execute; document **N/A** vs legacy ticket title |

### Detailed requirements (v1.4)

#### A. Preconditions and dependencies

* Enumeration drift check vs **[KS-991](https://gendvn.atlassian.net/browse/KS-991)** before claiming pass/fail.
* Vendor-approved **read-only** query or filter for first live run; **no** destructive SQL.

#### B. When `read_data` is absent (default through May 2026)

* Publish matrix cells **S** with rationale; keep **High-risk tool checklist** row open with status **Planned**.
* **Do not** substitute `list_table` / `describe_table` flows—these are **not** v1.4 tools.

#### C. When `read_data` is present

* **Prompt (example):** *Using `read_data`, return a small authorized sample of fund-related rows (e.g. first 10 rows per vendor-approved filter).*
* Validate structured columns, **row cap**, truncation flags, and **authorization** (black-box—no Dynamo UI).
* **Errors:** malformed/out-of-scope queries must fail with explicit errors—no broad dumps.

#### D. Matrix

* Guide **section 6** row **5.5 Tabular read (`read_data`)** — coordinate with **[KS-993](https://gendvn.atlassian.net/browse/KS-993)** (all non–n/a cells **S** until live, or **P**/**F** once executed).

### UI/UX & front-end considerations (v1.4)

* Never paste raw tokens; redact tabular rows per guide section 8 before Jira upload.

### Acceptance criteria (BDD, v1.4)

_Scenario 1 — Happy path_

* **Given** `read_data` is **registered** and an authorized sample query exists
* **When** the tester runs section **5.5**
* **Then** returned rows are structured, capped, and in-scope; repeat-call stability documented

_Scenario 2 — Error path_

* **Given** invalid or out-of-scope query
* **When** invoked
* **Then** schema/API error—no unrelated table dump

_Scenario 3 — Edge case (default today)_

* **Given** `read_data` is **not** registered
* **When** the ticket is executed for v1.4
* **Then** outcomes are **S** with written rationale and checklist row remains **Planned/tracked**

### Definition of Done (v1.4)

Unchanged from the **Definition of Done** block in the original description above; for QA execution, add: **5.5** matrix + checklist state published; if skipped, link enumeration proof from **[KS-991](https://gendvn.atlassian.net/browse/KS-991)**.