*Ticket Title:* {{Dynamo MCP QA - Map domain objects per tool and outbound data paths (MCP black box)}}

*Jira:* [KS-992|https://gendvn.atlassian.net/browse/KS-992] | *Epic:* Dynamo MCP — Discovery & Scope Enumeration

*User Story:*

{quote}As an *Internal QA Tester*, I want *a map of which domain objects each tool appears to touch (from names and outputs) and which tools could enable outbound / LLM-mediated exfiltration* so that *security tests (CHAIN, PIJ) and section 1.4 high-risk tracking target the right flows*.{quote}

*Overview:*

Delivers section 4.3 under *black-box* rules: infer "funds, notes, documents, ratings, activity" from tool behavior only — *no Dynamo schema documentation*. Identify exfiltration paths; *behaviorally* validate {{search_aloha_funds}} scope (e.g. vs {{get_funds}}), not UI entitlements.

*Detailed Requirements:*

* Document domain mapping from tool names/responses (e.g. {{get_documents}} → documents; {{get_rating_*}} → ratings).
* List tools with outbound or LLM-mediated behavior ({{llm_text_analysis}}, {{analyze_notes}}); call out *section 1.4* discovery/tabular tools.
* Explicitly record hypothesis and evidence for {{search_aloha_funds}} scoping using MCP-only checks.

*UI/UX & Front-End Considerations:*

* Diagram recommended (Mermaid) if 3+ entities or branches per BA skill.

{noformat}erDiagram
    FUND {
        uniqueidentifier ID PK
        nvarchar Name
        nvarchar FundManagerName
        nvarchar PipelineStatus
        nvarchar AssetClassName
        nvarchar SubAssetClassName
        nvarchar ResponsibleName
        nvarchar Description
        datetime LastModified
    }

    ACTIVITY {
        uniqueidentifier ID PK
        nvarchar Subject
        nvarchar Body_PlainText
        datetime Date
        nvarchar Activitycategories
        nvarchar Companies
        nvarchar Contacts
        nvarchar Funds
        bit Solovis
        bit DAIIsAutoTagged
        nvarchar KSrepresentative_s
        decimal Cashyieldofportfolio
    }

    DOCUMENT {
        uniqueidentifier ID PK
        nvarchar Title
        nvarchar FileName
        nvarchar Documentcategories
        datetime Documentdate
        nvarchar Funds
        nvarchar Companies
        nvarchar Content
    }

    CONTACT {
        uniqueidentifier ID PK
        nvarchar Name
    }

    ALOHA_FUND {
        string fund_id PK
        string fund_name
        string manager_name
        string source
        string fund_type
    }

    RATING_SUMMARY {
        string id PK
        string source
        integer edge
        integer organization
        integer track_record
        integer total_rating
        integer average_conviction
    }

    RATING_DETAIL {
        string id PK
        string source
        string user_upn
    }

    LLM_ANALYSIS {
        string type
        string summary
        string highlights
        string comparison
    }

    FUND ||--o{ ACTIVITY : "linked via Funds field"
    FUND ||--o{ DOCUMENT : "linked via Funds field"
    ACTIVITY ||--o{ CONTACT : "linked via Contacts field"
    DOCUMENT ||--o{ CONTACT : "linked via Contacts field"
    ACTIVITY }o--|| FUND : "Ref_Fund FK"
    ALOHA_FUND ||--o| RATING_SUMMARY : "fund_id + source"
    ALOHA_FUND ||--o{ RATING_DETAIL : "fund_id + source + user"
    ACTIVITY ||--o| LLM_ANALYSIS : "Body_PlainText sent to LLM"
    ALOHA_FUND }o--o| FUND : "same manager, different system"{noformat}

*Acceptance Criteria (BDD):*

_Scenario 1 — Happy path_

* *Given* enumeration is complete
* *When* the mapping is reviewed with engineering
* *Then* upstream map is approved for use in E4

_Scenario 2 — Error path_

* *Given* a tool's backend is unknown
* *When* vendor cannot clarify
* *Then* risk is recorded as "assumption pending" with test limitations

_Scenario 3 — Edge case_

* *Given* a new tool or entity surface appears post-deploy
* *When* registered on server
* *Then* mapping doc is updated before regression

*Definition of Done:* _(verbatim block above)_

----

## Updated requirements — guide v1.4 (8-tool MCP inventory)

**Effective May 2026 (customer confirmation).** Aligns this ticket with `Dynamo Server/Test Guide/dynamo-mcp-testing-guide_v1.4.md` and `Dynamo Server/Test Result/dynamo_mcp_testing_stories_v1.2.md`. **Does not replace** earlier text in this description.

**8-tool customer-confirmed surface**

| # | Tool | Availability (May 2026) |
| --- | --- | --- |
| 1 | `analyze_notes` | Available |
| 2 | `get_activity` | Available |
| 3 | `get_documents` | Available |
| 4 | `get_fund_description` | Available |
| 5 | `get_funds` | Available |
| 6 | `get_notes` | Available |
| 7 | `llm_text_analysis` | Available |
| 8 | `read_data` | **Planned** — add to regression when registered in the client |

**Out of scope for v1.4:** `describe_table`, `get_rating_details`, `get_rating_summary`, `list_table`, `search_aloha_funds`.

**section 1.4 (guide v1.4):** only `read_data` is HIGH risk. Mark `read_data` scenarios **S (skipped)** until the tool is registered.

**Ticket-specific expectations (stories v1.2):**
Map domain objects for the **8-tool** set; LLM egress on `analyze_notes` / `llm_text_analysis`; cross-tool fund scope via `get_*` tools (not `search_aloha_funds`).
